import { Attribute } from "./Attribute.js";

/**
 * A class representing a prestige attribute, which extends the base {@link Attribute} class.
 * This class can be used to create instances of prestige attributes with specific properties.
 */
export class PrestigeAttribute extends Attribute {}
